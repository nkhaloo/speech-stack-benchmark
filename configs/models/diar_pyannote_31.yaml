# Diarization — pyannote/speaker-diarization-3.1 (GPU primary; CPU-capable)
# MIT weights but GATED: accept conditions on huggingface.co once, then
# export HF_TOKEN for download; offline afterwards.
id: pyannote-3.1
family: pyannote
runtime: pyannote
model: pyannote/speaker-diarization-3.1
device: cuda              # set "cpu" for the CPU track (see cpu.yaml override)
license: MIT              # pyannote.audio library
weights_license: MIT
gated: true
offline_after_download: true
expected_vram_gb: 3
disk_size_gb: 0.1
deployment_notes: "Requires pyannote.audio 3.x; language-independent."
download:
  kind: hf_snapshot
  repo: pyannote/speaker-diarization-3.1
  extra_repos:
    - pyannote/segmentation-3.0
    - pyannote/wespeaker-voxceleb-resnet34-LM
